send_command:
  description: Send a Command To Myko Cloud
  target:
    entity:
      integration: myko
      domain: light
  fields:
    value:
      name: value
      description: value you want to send
      required: true
      example: "on"
    functionClass:
      name: functionClass
      description: functionClass you want to send
      required: true
      example: "power"
    functionInstance:
      name: functionInstance
      description: functionInstance you want to send
      required: false
      example: "primary"
